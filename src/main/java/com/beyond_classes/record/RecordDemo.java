package com.beyond_classes.record;

public class RecordDemo {
    /*
        Record
            - encapsulated classes, but without boilerplate code
            - the encapsulation is secured
            - constructor, getters, toString(), equals(), hashCode() are generated
            - record cannot have explicit instance fields
            - record can have static fields and methods
            - record can have instance methods

     */
}
